<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Canpay_operator"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <!-- Launcher activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities with android:exported="true" -->
        <activity android:name=".PinCodeActivity" android:exported="true" />
        <activity android:name=".ReceiptDetailActivity" android:exported="true" />
        <activity android:name=".AssignedFailedActivity" android:exported="true" />
        <activity android:name=".AssignedSuccessActivity" android:exported="true" />
        <activity android:name=".PinLoginActivity" android:exported="true" />
        <activity android:name=".ConfirmSetPinActivity" android:exported="true" />
        <activity android:name=".AccountFailureActivity" android:exported="true" />
        <activity android:name=".HomeActivity" android:exported="true" />
        <activity android:name=".AccountSuccessActivity" android:exported="true" />
        <activity android:name=".ConfirmPinCodeActivity" android:exported="true" />
        <activity android:name=".TakePhotoActivity" android:exported="true" />
        <activity android:name=".NICEntryActivity" android:exported="true" />
        <activity android:name=".NameActivity" android:exported="true" />
        <activity android:name=".PhoneNoActivity" android:exported="true" />
        <activity android:name=".OtpActivity" android:exported="true" />
        <activity android:name=".LoginActivity" android:exported="true" />

    </application>

</manifest>
